# 이넘(Enums)
* 특정 값들의 집합을 의미하는 자료형
* 초기값을 지정하지 않으면 숫자형 이넘으로 취급해서 0부터 차례로 1씩 증가한다.
* 문자형 이넘은 첫 번째 값 부터 마지막 값까지 전부 초기화 해야 한다.

# 제네릭(Generics)
* C#, Java 등의 언어에서 재사용성이 높은 컴포넌트를 만들 때 자주 활용된다.
* 한가지 보다 여러 가지 타입에서 동작하는 컴포넌트를 만들 때 사용된다.